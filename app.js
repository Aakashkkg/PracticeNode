const data = require("./calculate/data.json")
const { x, sumdata, mul } = require("./calculate/index.js")

// console.log(global)
// console.log(x)
console.log(sumdata(7, 8))
console.log(mul(7, 8))
// console.log(data)

// module type used in react tech its use import export feature
// and run on strict mode and have aysc behavior

// by default node js type is commonjs use module.exports feature not run on strict mode sync behavior